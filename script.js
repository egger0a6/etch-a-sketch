const viewport = querySelector("#viewport");
let tileX = 16;
let tileY = 16;
let size = 16;

function createGrid() {
    let grid = document.createElement("div");
    grid.setAttribute("id", "grid-container");
    grid.setAttribute("sytle")

    let i;
    for (i = 0; i < size; i++) {

    }
}